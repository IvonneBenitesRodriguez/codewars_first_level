### Using Multiple If...Else Statements:
The use of Else if Statements is related with the conditions that you want to add to a specific function.<br/>
While the if...else executes a statement with a specific condition which is true. But in case, we want to add a condition that is false we need to add else clause to that function, so it can be executed. <br/>
In the case of using Multiple if...else statements, these can be nested to create an else-if clause.<br/><br/>
🐇 Syntax: <br/><br/>
<strong>function test( i ){</strong><br/>
<strong>if(condition1)</strong>{<br/>
            return statement1; <br/>
}<strong>else if(condition2)</strong>{ <br/>
            return statement2;<br/>
}<strong>else if(condition3)</strong>{<br/>
            return statement3;<br/>
}<strong>else{</strong><br/><>
            return statementN;<br/>
}<br/>
}<br/>