### .toUpperCase() 
This method returns a string converted into capital letters.<br/>
🐇 Syntax:<br/>
.toUpperCase( ); <br/>
It has no parameters inside.<br/>
The method does not affect the value of a string itself since JS strings are immutable.<br/>